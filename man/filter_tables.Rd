% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-tables.R
\name{filter_tables}
\alias{filter_tables}
\title{Filter Tables by Criteria}
\usage{
filter_tables(
  id_count = NULL,
  item_count = NULL,
  resp_count = NULL,
  sparsity = NULL,
  has_date = NULL,
  has_rt = NULL,
  has_rater = NULL
)
}
\arguments{
\item{id_count}{A numeric vector of two values specifying the range for the number of unique IDs
(e.g., \code{c(100, 1000)}).}

\item{item_count}{A numeric vector of two values specifying the range for the number of unique items.}

\item{resp_count}{A numeric vector of two values specifying the range for the total number of responses.}

\item{sparsity}{A numeric vector of two values specifying the range for sparsity (between 0 and 1).}

\item{has_date}{Logical; \code{TRUE} to include tables with a \code{date} column, \code{FALSE} to exclude them,
or \code{NULL} to ignore this criterion.}

\item{has_rt}{Logical; \code{TRUE} to include tables with a \code{rt} column, \code{FALSE} to exclude them,
or \code{NULL} to ignore this criterion.}

\item{has_rater}{Logical; \code{TRUE} to include tables with a \code{rater} column, \code{FALSE} to exclude them,
or \code{NULL} to ignore this criterion.}
}
\value{
A character vector of table names that match the criteria. If no tables match, it prints a message
and returns an empty character vector.
}
\description{
Filters the metadata to return the names of tables that match the specified criteria.
}
\examples{
# Example 1: Filter tables with ID count between 100 and 1000
filter_tables(id_count = c(100, 1000))

# Example 2: Filter tables with multiple numeric criteria
filter_tables(id_count = c(100, 1000), sparsity = c(0.1, 0.5))

# Example 3: Filter tables with specific columns
filter_tables(has_date = TRUE, has_rt = TRUE)

# Example 4: Combine numeric and column presence criteria
filter_tables(id_count = c(100, 1000), has_date = TRUE)

}
