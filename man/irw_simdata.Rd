% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{irw_simdata}
\alias{irw_simdata}
\title{Simulate IRW-compliant dichotomous item response data}
\usage{
irw_simdata(
  n_id = 1000,
  n_item = 20,
  model = "1PL",
  a = NULL,
  d = NULL,
  g = NULL,
  theta = NULL,
  theta_mean = 0,
  theta_sd = 1,
  seed = NULL,
  return_params = FALSE
)
}
\arguments{
\item{n_id}{Number of respondents. Ignored if \code{theta} is provided. Default is 1000.}

\item{n_item}{Number of items. Default is 20.}

\item{model}{Character string: one of \code{"1PL"}, \code{"2PL"}, or \code{"3PL"}. Default is \code{"1PL"}.}

\item{a}{Optional vector of item discriminations. If \code{NULL}, sampled from lognormal(0.2, 0.2) (ignored for 1PL).}

\item{d}{Optional vector of item difficulties. If \code{NULL}, sampled from N(0, 1).}

\item{g}{Optional vector of guessing parameters. Required for 3PL; if \code{NULL}, sampled from Beta(5, 17).}

\item{theta}{Optional vector of person abilities. If provided, overrides \code{n_id}, \code{theta_mean}, and \code{theta_sd}.}

\item{theta_mean}{Mean of theta distribution (if \code{theta} not supplied). Default is 0.}

\item{theta_sd}{Standard deviation of theta distribution. Default is 1.}

\item{seed}{Optional integer for reproducibility.}

\item{return_params}{Logical. If \code{TRUE}, returns a list with data and parameters. Default is \code{FALSE}.}
}
\value{
A data frame with columns \code{id}, \code{item}, \code{resp}, or a list with additional parameters if \code{return_params = TRUE}.
}
\description{
Simulates item response data from 1PL, 2PL, or 3PL logistic IRT models.
Returns a long-format data frame with \code{id}, \code{item}, and \code{resp} columns.
}
\examples{
\dontrun{
# 1PL (default)
dat <- irw_simdata(n_item = 5)

# 3PL with latent traits drawn from N(-0.5, 1)
sim <- irw_simdata(n_item = 5, model = "3PL", theta_mean = -0.5, return_params = TRUE)
head(sim$data)
}

}
