% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disk-cache.R
\name{irw_cache_info}
\alias{irw_cache_info}
\title{List the cached tables}
\usage{
irw_cache_info()
}
\value{
A tibble with one row per cached file: \code{kind} (\code{"tables"} or
\code{"itemtext"}), \code{table}, \code{hash} (Redivis' content hash, which decides
whether the copy is current), \code{version} (the dataset release it was
fetched from), \code{reference}, \code{size_mb}, \code{fetched_at} and \code{path}.
}
\description{
\code{irw_fetch()} and \code{irw_itemtext()} keep each table they download so that
later sessions do not export it again. A copy is used for as long as the
table is unchanged on Redivis, and replaced when it changes. See
\code{\link[=irw_cache_dir]{irw_cache_dir()}} for where the files are and how to switch this off.
}
\examples{
\dontrun{
info <- irw_cache_info()
sum(info$size_mb)
}
}
