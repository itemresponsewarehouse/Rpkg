% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collections.R
\name{irw_collections}
\alias{irw_collections}
\title{List IRW Collections}
\usage{
irw_collections(kind = NULL)
}
\arguments{
\item{kind}{Optional character vector. Filter to \code{"design"}, \code{"instrument"}
and/or \code{"construct"}.}
}
\value{
A tibble, one row per collection.
}
\description{
Returns the collection registry: one row per collection, with its kind,
definition, the rule that produced it, and how complete its coverage is.
}
\details{
Collections are labelled groupings of IRW tables — study designs (\code{rct},
\code{clustered}, \code{q_matrix}), instrument families (\code{big_five}, \code{promis}) and
constructs (\code{depression}, \code{math}). A table can belong to any number of them.
}
\section{Coverage}{

\code{coverage} says how much of the warehouse the collection's rule actually
searched, and it matters when you need "all" of something:
\describe{
\item{\code{metadata-complete}}{The rule read \code{irw_metadata()}, so it saw every
documented table. Note this is not the same as the whole warehouse —
\code{irw_metadata()} itself has gaps.}
\item{\code{tagged-subset-only}}{The rule read the tags table, which covers
roughly 62\% of documented tables, and coverage is much lower in the newer
warehouses. These collections are biased toward older tables and should
not be described as exhaustive.}
\item{\code{curated-only}}{Every member was chosen by hand.}
}
}

\examples{
\dontrun{
irw_collections()
irw_collections(kind = "instrument")
}
}
\seealso{
\code{\link[=irw_collection]{irw_collection()}}, \code{\link[=irw_collection_members]{irw_collection_members()}}, \code{\link[=irw_filter]{irw_filter()}}
}
